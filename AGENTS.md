# Learningflow AI 开发规则

本文件只保存每次开发都必须知道的稳定规则和文档路由。产品事实、架构细节、实验记录和历史过程按职责放入对应文档，不把 `AGENTS.md` 扩写成项目百科。

## 1. 开始任务

1. 先读 `PROJECT_STATE.md`，确认当前阶段、正在验证什么、下一步是什么。
2. 再按任务读取对应产品 / 架构 / 工程文档，不为“建立上下文”遍历整个仓库。
3. 修改仓库文件前做 Git 开场检查：

   ```bash
   git status --short
   git status -sb
   git log --oneline --decorate -6
   ```

4. 如果已经配置 `origin`，再执行 `git fetch origin` 后重新检查分支关系：
   - 与 `origin/main` 一致：正常开发；
   - 本地只落后且可 fast-forward：只使用 `git pull --ff-only`；
   - 本地只领先：不要 pull，先确认领先提交属于正常本地开发；
   - 本地与远端 diverged：停止普通开发，先分析分叉，不自动 merge / rebase / force push；
   - 工作区已有未提交修改：先识别来源并保留，不擅自 reset、clean、stash 或覆盖。

### 文档路由

| 需要了解 | 优先读取 |
| --- | --- |
| 当前阶段、正在做什么、下一步 | `PROJECT_STATE.md` |
| 长期关键决策及原因 | `DECISIONS.md` |
| 产品范围、用户、首版边界 | `docs/product/产品基线.md` |
| 真实日常学习与语音/网页交接 | `docs/product/真实学习流程.md` |
| ChatGPT-first 总体定位、Context/Evidence、Skill/MCP 边界 | `docs/architecture/长期学习操作系统.md` |
| 当前系统架构、数据与模块建议 | `docs/architecture/系统架构.md` |
| 当前 M0 最小实现边界 | `docs/architecture/M0最小闭环设计.md` |
| 文档职责、拆分和归档 | `docs/development/文档治理.md` |
| Git 提交与分支规则 | `docs/development/Git工作流.md` |
| Milestone / Issue / Discussion / 状态收口 | `docs/development/项目管理.md` |
| 原始长对话 / 未收口思考 | `resources/raw/`，仅在追溯时读取 |

## 2. 产品硬边界

- **Learningflow 不是题库，也不是单纯错题本。** 核心资产是连续 Learning Evidence，以及围绕它运行的诊断、教学、复习、监督和反思闭环。
- **首个学科是小学英语，但通用内核不得写死英语规则。** 学科差异进入 Subject Pack；不要为了未来扩展提前建设复杂插件平台。
- **首版继续复用 ChatGPT App 作为主要语音老师。** 未经过真实设备验证前，不自行转向自建实时语音系统。
- **Server-first，客户端轻量。** 调度、通知、复习和长期状态不能依赖当前浏览器或 ChatGPT 会话保持在线。
- **Test 与 Real 数据从第一阶段隔离。** 测试任务、学习状态、报告、反思和通知不得污染真实学生数据。
- **外部工具按需发现，不维护固定全平台连接器集合。** 豆包、元宝、微信、抖音、小红书等只作为候选来源；实际任务需要时再研究当前可用接入方式。

## 3. 学习数据原则

- 原始证据、提取结果、分析判断、当前状态必须分层保存。
- 模型输出默认是建议、分析或派生结论，不得无依据覆写原始作答、照片、录音等事实。
- OCR / 转写 / 判分 / 诊断允许修订和重算；变更后应能识别哪些 Learner State、Review、Reflection 或 Report 需要重新计算或标记过期。
- 一次作答必须能回答：谁、做了什么题、原始答案是什么、是否有提示/讲解、何时发生、来自什么材料、是否独立。
- “刚讲完答对”与“隔一段时间独立答对”是不同强度证据。
- 错因是候选假设；证据不足时保留不确定性，不通过一题把多个关联知识点一起降级。
- 学习状态必须能追溯到 Evidence，不能只保存一个没有依据的 mastery 百分比。

## 4. 分析与实施边界

- 用户要求分析、比较、研究或排查时，默认先读取、验证和提出方案；这不自动授权扩大产品范围或开始无关重构。
- 用户明确说“创建”“开始做”“按这个方案实现”等，或一开始就给出可执行要求时，进入实施阶段；已确认范围内的代码、测试和文档同步不重复确认。
- 实施中出现会改变产品边界、数据事实、隐私范围、测试/真实隔离或验收标准的新选择时，只暂停该选择对应部分，不擅自替用户决定。

### 实现原则

- 优先选择当前明确需求下最简单、直接、可维护的方案。
- 不过度设计，不提前增加假设中的抽象层、配置系统、插件系统和兼容层。
- 不写过度防御性代码；只处理现实中可能发生、已有证据支持或失败代价明显较高的异常。
- 新项目不为不存在的旧版本、旧 API、旧数据格式提前做兼容。
- 测试覆盖核心学习事实、关键流程、已发现 Bug 和重要隔离边界即可，不为了覆盖率测试无业务价值实现细节。
- 能删除复杂度时不要增加中间层；能在现有模块直接实现时，不为了“以后可能复用”先造框架。

## 5. 工程治理门禁

- **真实 Bug 尽量留下防复发资产。** 能稳定自动表达时补回归测试，并检查同类路径是否有相同问题。
- **新表 / 字段 / 状态先证明必要性。** 能从已有事实推导时不再保存一份容易漂移的状态；确需持久化时明确其更新、重算和清理责任。
- **AI 不为让测试变绿而放宽学习规则。** 如果必须改变 Evidence 强度、数据隔离、隐私边界或验收标准才能通过测试，应回到方案讨论。
- **真实学习数据属于敏感数据。** 不进入 Git、测试 fixture、日志示例或公开 Issue；测试优先使用合成数据。
- **外部调用遵守最小数据原则。** 搜索或外部工具默认只获得完成当前任务所需的最少上下文；学生身份、完整作业材料、音频等不得无必要发送。

## 6. 文档与事实源

- `PROJECT_STATE.md`：只记录现在的阶段、已验证事实和下一步。
- `DECISIONS.md`：记录长期重要选择“为什么这样定”。
- `docs/product/`：当前产品目标、真实流程和业务规则。
- `docs/architecture/`：当前架构、数据边界和经过验证的技术事实。
- `docs/development/`：Git、文档、测试等工程规则。
- `docs/archive/`：已经被取代但仍有追溯价值的正式文档。
- `resources/raw/`：本机原始聊天、思考和外部原文，不进入 Git，也不是当前事实源。

同一事实尽量只有一个权威来源。实现、数据模型、流程或技术基线变化时，在同一任务中更新对应权威文档；不要在文档末尾不断追加“最新规则”而保留前文冲突内容。

## 7. Git 与任务收口

- Git 是开发历史事实源；`PROJECT_STATE.md` 只保留当前状态，不复制完整提交历史。
- 一个可独立验收、可独立回滚的逻辑单元完成后形成 commit/checkpoint，不把多个已完成事项长期堆在 dirty worktree。
- checkpoint message 使用 `checkpoint: <阶段说明>`；普通文档/维护变更可用清晰的 Conventional Commit 风格。
- 不使用 `git reset --hard`、`git clean`、`git stash` 擅自处理用户已有改动。
- 当前仓库已配置公开远端 `origin=https://github.com/Roc-kit/Learningflow.git`，本地 `main` 跟踪 `origin/main`；远端可见性或归属未来如需改变，必须显式处理，不自行迁移或改名。

## 8. 当前阶段特有约束

Stage 0 的最高优先级是验证 M0 真实闭环，而不是先搭“看起来完整”的工程：

```text
教学材料进入 ChatGPT
→ Assessment Skill 进入短验证
→ ChatGPT 一次一题
→ 孩子用文字或语音直接回答
→ 可用时通过 MCP 保存结构化 Learning Evidence
→ 老师基于具体答案继续教学
```

ChatGPT 是孩子侧主教学界面。Learningflow Web 只在图片、复杂交互、录音、长文本或受控采集等 ChatGPT 本身不适合的场景按需调用；不要为了结构化而把普通问答强制搬到网页。

因此当前不要先做：

- 完整全学段知识图谱；
- BKT / IRT / FSRS 等复杂学习状态算法；
- 完整多渠道通知平台；
- 固定全平台连接器集合；
- 微服务拆分；
- 自研实时语音老师；
- 为尚不存在的生产版本预建复杂升级兼容系统。

## 9. 完成前最低检查

- 当前验收目标是否真的满足，而不是只完成代码层动作；
- `git diff --check` 是否通过；
- 与改动风险相称的测试 / 手工验证是否完成；
- `PROJECT_STATE.md`、`DECISIONS.md` 和对应产品/架构文档是否需要同步；
- 是否把真实学生数据、凭证或原始私密材料误纳入 Git。

只有多数相关任务都必须知道、或反复踩坑且代价较大的规则，才加入本文件；任务专用流程放到对应文档，避免 `AGENTS.md` 持续膨胀。
